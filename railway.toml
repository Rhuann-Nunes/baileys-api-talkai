[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "node -r ./paths.js dist/main.js"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure" 